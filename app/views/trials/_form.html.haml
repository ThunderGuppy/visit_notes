= form_for @trial do |f|
  - if @trial.errors.any?
    #error_explanation
      %h2= "#{pluralize(@trial.errors.count, "error")} prohibited this trial from being saved:"
      %ul
        - @trial.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
